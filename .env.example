# Public — safe to expose to the browser (protected by RLS, not secrets)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only — never prefix with VITE_, never expose to the client.
# Bypasses RLS entirely; used only for admin-provisioning operations (Fase 4+).
SUPABASE_SERVICE_ROLE_KEY=
